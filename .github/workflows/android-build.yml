# ===================================================================
# File: .github/workflows/android-build.yml
# Revisione Finale Definitiva (Standard Industriale, Auto-Bootstrap):
# - Ignora i file gradlew/wrapper locali.
# - Usa l'azione ufficiale 'setup-gradle' per creare un ambiente Gradle pulito.
# - Esegue la build con il comando 'gradle', non './gradlew'.
# - Funziona indipendentemente dalla versione di Ubuntu Runner.
# ===================================================================
name: Android CI Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    # Usiamo ubuntu-22.04 perché sappiamo che ci assegna un Runner velocemente
    runs-on: ubuntu-22.04

    steps:
    # 1. Scarica il codice del repository
    - name: Checkout del codice
      uses: actions/checkout@v4

    # 2. Imposta l'ambiente Java (JDK 17)
    - name: Impostazione di JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    # 3. Imposta un ambiente Gradle pulito e funzionante
    # QUESTO È IL PASSO CHE RISOLVE TUTTO. Installa un Gradle ufficiale
    # e lo mette a disposizione, ignorando i nostri file locali.
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3

    # 4. Esegue la compilazione usando il comando 'gradle' fornito da Setup Gradle
    # Questo comando NON usa il file ./gradlew che non viene trovato.
    - name: Compilare con Gradle
      run: gradle build --stacktrace

    # 5. Salva l'APK generato per il download
    - name: Caricare l'artefatto APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug-apk
        if-no-files-found: error
        path: app/build/outputs/apk/debug/app-debug.apk
